name: Build Extension

on:
  workflow_dispatch:
    inputs:
      release:
        description: 'Create pre-release'
        required: false
        default: false
        type: boolean

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Enable Corepack and install correct Yarn version
        run: |
          corepack enable
          corepack prepare yarn@4.7.0 --activate
          # Verify Yarn version
          yarn --version
      
      - name: Install dependencies
        run: yarn install --frozen-lockfile
      
      - name: Build Chrome extension
        run: yarn build
      
      - name: Package Chrome extension
        run: |
          rm -f tusk-chrome-extension.zip
          cd extension
          zip -r ../tusk-chrome-extension.zip *
          cd ..
      
      - name: Build Firefox extension
        run: yarn build:ff
      
      - name: Package Firefox extension
        run: |
          rm -f tusk-firefox-extension.zip
          cd extension
          zip -r ../tusk-firefox-extension.zip *
          cd ..
      
      - name: Upload Chrome artifact
        uses: actions/upload-artifact@v4
        with:
          name: tusk-chrome-extension
          path: tusk-chrome-extension.zip
      
      - name: Upload Firefox artifact
        uses: actions/upload-artifact@v4
        with:
          name: tusk-firefox-extension
          path: tusk-firefox-extension.zip
      
      - name: Create pre-release
        if: github.event.inputs.release == 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: Build ${{ github.run_number }}
          draft: false
          prerelease: true
          files: tusk-chrome-extension.zip,tusk-firefox-extension.zip
          body: |
            ## Automated Build ${{ github.run_number }}
            
            - Build targets: Chrome, Firefox
            - Build date: ${{ github.event.timestamp }}
            - Commit: ${{ github.sha }}